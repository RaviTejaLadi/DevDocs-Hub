import type { InterviewQA } from '../types';

export const dsaQuestions: InterviewQA[] = [
  {
    id: 'dsa-1',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Two Sum',
    answer: [
      '**Problem:** Given an array of integers `nums` and an integer `target`, return indices of the two numbers such that they add up to `target`.',
      '',
      '**Concept:** Use a hash map to store the complement of each number. For each number, check if its complement exists in the hash map.',
      '',
      '**Solution:**',
      '```javascript',
      'function twoSum(nums, target) {',
      '  const numMap = new Map();',
      '',
      '  for (let i = 0; i < nums.length; i++) {',
      '    const complement = target - nums[i];',
      '    if (numMap.has(complement)) {',
      '      return [numMap.get(complement), i];',
      '    }',
      '    numMap.set(nums[i], i);',
      '  }',
      '  return [];',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(n)',
    ].join('\n'),
  },
  {
    id: 'dsa-2',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Valid Parentheses',
    answer: [
      "**Problem:** Given a string `s` containing just the characters '(', ')', '{', '}', '[' and ']', determine if the input string is valid.",
      '',
      '**Concept:** Use a stack to keep track of opening brackets. When encountering a closing bracket, check if it matches the most recent opening bracket.',
      '',
      '**Solution:**',
      '```javascript',
      'function isValid(s) {',
      '  const stack = [];',
      "  const mapping = { ')': '(', '}': '{', ']': '[' };",
      '',
      '  for (let char of s) {',
      '    if (char in mapping) {',
      '      if (stack.length === 0 || stack.pop() !== mapping[char]) return false;',
      '    } else {',
      '      stack.push(char);',
      '    }',
      '  }',
      '  return stack.length === 0;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(n)',
    ].join('\n'),
  },
  {
    id: 'dsa-3',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Reverse Linked List',
    answer: [
      '**Problem:** Given the head of a singly linked list, reverse the list and return the reversed list.',
      '',
      '**Concept:** Use three pointers: prev, current, and next. Reverse the links iteratively.',
      '',
      '**Solution:**',
      '```javascript',
      'function reverseList(head) {',
      '  let prev = null;',
      '  let current = head;',
      '',
      '  while (current !== null) {',
      '    const next = current.next;',
      '    current.next = prev;',
      '    prev = current;',
      '    current = next;',
      '  }',
      '  return prev;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-4',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Maximum Subarray',
    answer: [
      '**Problem:** Find the contiguous subarray with the largest sum and return its sum.',
      '',
      "**Concept:** Use Kadane's algorithm: keep track of maximum sum ending at current position and overall maximum sum.",
      '',
      '**Solution:**',
      '```javascript',
      'function maxSubArray(nums) {',
      '  let maxSoFar = nums[0];',
      '  let maxEndingHere = nums[0];',
      '',
      '  for (let i = 1; i < nums.length; i++) {',
      '    maxEndingHere = Math.max(nums[i], maxEndingHere + nums[i]);',
      '    maxSoFar = Math.max(maxSoFar, maxEndingHere);',
      '  }',
      '  return maxSoFar;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-5',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Merge Two Sorted Lists',
    answer: [
      '**Problem:** Merge two sorted linked lists and return it as a sorted list.',
      '',
      '**Concept:** Use a dummy node to simplify the merging process. Compare nodes from both lists and attach the smaller one.',
      '',
      '**Solution:**',
      '```javascript',
      'function mergeTwoLists(l1, l2) {',
      '  const dummy = { next: null };',
      '  let current = dummy;',
      '',
      '  while (l1 !== null && l2 !== null) {',
      '    if (l1.val <= l2.val) {',
      '      current.next = l1;',
      '      l1 = l1.next;',
      '    } else {',
      '      current.next = l2;',
      '      l2 = l2.next;',
      '    }',
      '    current = current.next;',
      '  }',
      '  current.next = l1 || l2;',
      '  return dummy.next;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n + m)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-6',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Binary Search',
    answer: [
      '**Problem:** Given an array of integers `nums` sorted in ascending order, and an integer `target`, write a function to search `target` in `nums`.',
      '',
      '**Concept:** Use binary search to find the target in O(log n) time by repeatedly dividing the search space in half.',
      '',
      '**Solution:**',
      '```javascript',
      'function search(nums, target) {',
      '  let left = 0;',
      '  let right = nums.length - 1;',
      '',
      '  while (left <= right) {',
      '    const mid = Math.floor((left + right) / 2);',
      '    if (nums[mid] === target) return mid;',
      '    else if (nums[mid] < target) left = mid + 1;',
      '    else right = mid - 1;',
      '  }',
      '  return -1;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(log n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-7',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Best Time to Buy and Sell Stock',
    answer: [
      '**Problem:** Find the maximum profit from buying and selling a stock once.',
      '',
      '**Concept:** Track the minimum price seen so far and calculate profit at each day.',
      '',
      '**Solution:**',
      '```javascript',
      'function maxProfit(prices) {',
      '  let minPrice = prices[0];',
      '  let maxProfit = 0;',
      '',
      '  for (let i = 1; i < prices.length; i++) {',
      '    minPrice = Math.min(minPrice, prices[i]);',
      '    maxProfit = Math.max(maxProfit, prices[i] - minPrice);',
      '  }',
      '  return maxProfit;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-8',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Valid Anagram',
    answer: [
      '**Problem:** Given two strings `s` and `t`, return true if `t` is an anagram of `s`.',
      '',
      '**Concept:** Count frequency of each character in both strings and compare.',
      '',
      '**Solution:**',
      '```javascript',
      'function isAnagram(s, t) {',
      '  if (s.length !== t.length) return false;',
      '',
      '  const charCount = {};',
      '  for (let char of s) charCount[char] = (charCount[char] || 0) + 1;',
      '  for (let char of t) {',
      '    if (!charCount[char]) return false;',
      '    charCount[char]--;',
      '  }',
      '  return true;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(n)',
    ].join('\n'),
  },
  {
    id: 'dsa-9',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Climbing Stairs',
    answer: [
      "**Problem:** You're climbing a staircase with `n` steps. Each time you can climb 1 or 2 steps. How many distinct ways can you climb to the top?",
      '',
      '**Concept:** This is a Fibonacci sequence problem. Ways to reach step n = ways to reach (n-1) + ways to reach (n-2).',
      '',
      '**Solution:**',
      '```javascript',
      'function climbStairs(n) {',
      '  if (n <= 2) return n;',
      '',
      '  let prev2 = 1; // ways to reach step 1',
      '  let prev1 = 2; // ways to reach step 2',
      '',
      '  for (let i = 3; i <= n; i++) {',
      '    const current = prev1 + prev2;',
      '    prev2 = prev1;',
      '    prev1 = current;',
      '  }',
      '  return prev1;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-10',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Binary Tree Inorder Traversal',
    answer: [
      "**Problem:** Given the root of a binary tree, return the inorder traversal of its nodes' values.",
      '',
      '**Concept:** Inorder traversal visits nodes in: left subtree → root → right subtree order.',
      '',
      '**Solution (Recursive):**',
      '```javascript',
      'function inorderTraversal(root) {',
      '  const result = [];',
      '  function inorder(node) {',
      '    if (node === null) return;',
      '    inorder(node.left);',
      '    result.push(node.val);',
      '    inorder(node.right);',
      '  }',
      '  inorder(root);',
      '  return result;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(h) where h is height of tree',
    ].join('\n'),
  },
  {
    id: 'dsa-11',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Contains Duplicate',
    answer: [
      '**Problem:** Given an integer array `nums`, return true if any value appears at least twice in the array.',
      '',
      '**Concept:** Use a Set to track seen elements. If an element is already in the set, we found a duplicate.',
      '',
      '**Solution:**',
      '```javascript',
      'function containsDuplicate(nums) {',
      '  const seen = new Set();',
      '  for (let num of nums) {',
      '    if (seen.has(num)) return true;',
      '    seen.add(num);',
      '  }',
      '  return false;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(n)',
    ].join('\n'),
  },
  {
    id: 'dsa-12',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Product of Array Except Self',
    answer: [
      '**Problem:** Given an integer array `nums`, return an array where each element is the product of all elements except itself.',
      '',
      '**Concept:** Calculate left products and right products separately, then combine them.',
      '',
      '**Solution:**',
      '```javascript',
      'function productExceptSelf(nums) {',
      '  const n = nums.length;',
      '  const result = new Array(n);',
      '',
      '  result[0] = 1;',
      '  for (let i = 1; i < n; i++) result[i] = result[i - 1] * nums[i - 1];',
      '',
      '  let rightProduct = 1;',
      '  for (let i = n - 1; i >= 0; i--) {',
      '    result[i] *= rightProduct;',
      '    rightProduct *= nums[i];',
      '  }',
      '  return result;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(1) excluding output array',
    ].join('\n'),
  },
  {
    id: 'dsa-13',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Maximum Depth of Binary Tree',
    answer: [
      '**Problem:** Given the root of a binary tree, return its maximum depth.',
      '',
      '**Concept:** Use recursion to find the maximum depth of left and right subtrees, then add 1 for current node.',
      '',
      '**Solution:**',
      '```javascript',
      'function maxDepth(root) {',
      '  if (root === null) return 0;',
      '  const leftDepth = maxDepth(root.left);',
      '  const rightDepth = maxDepth(root.right);',
      '  return Math.max(leftDepth, rightDepth) + 1;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(h) where h is height of tree',
    ].join('\n'),
  },
  {
    id: 'dsa-14',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Linked List Cycle',
    answer: [
      '**Problem:** Given head of a linked list, determine if the linked list has a cycle.',
      '',
      "**Concept:** Use Floyd's Cycle Detection Algorithm (tortoise and hare) with two pointers moving at different speeds.",
      '',
      '**Solution:**',
      '```javascript',
      'function hasCycle(head) {',
      '  if (head === null || head.next === null) return false;',
      '',
      '  let slow = head;',
      '  let fast = head.next;',
      '',
      '  while (slow !== fast) {',
      '    if (fast === null || fast.next === null) return false;',
      '    slow = slow.next;',
      '    fast = fast.next.next;',
      '  }',
      '  return true;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-15',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: '3Sum',
    answer: [
      '**Problem:** Given an integer array `nums`, return all unique triplets that sum to zero.',
      '',
      '**Concept:** Sort the array, then use two pointers for each element to find triplets.',
      '',
      '**Solution:**',
      '```javascript',
      'function threeSum(nums) {',
      '  const result = [];',
      '  nums.sort((a, b) => a - b);',
      '',
      '  for (let i = 0; i < nums.length - 2; i++) {',
      '    if (i > 0 && nums[i] === nums[i - 1]) continue;',
      '',
      '    let left = i + 1;',
      '    let right = nums.length - 1;',
      '',
      '    while (left < right) {',
      '      const sum = nums[i] + nums[left] + nums[right];',
      '      if (sum === 0) {',
      '        result.push([nums[i], nums[left], nums[right]]);',
      '        while (left < right && nums[left] === nums[left + 1]) left++;',
      '        while (left < right && nums[right] === nums[right - 1]) right--;',
      '        left++;',
      '        right--;',
      '      } else if (sum < 0) {',
      '        left++;',
      '      } else {',
      '        right--;',
      '      }',
      '    }',
      '  }',
      '  return result;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n²)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-16',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Search in Rotated Sorted Array',
    answer: [
      '**Problem:** Search for a target value in a rotated sorted array.',
      '',
      '**Concept:** Use modified binary search. One half of the array is always sorted.',
      '',
      '**Solution:**',
      '```javascript',
      'function search(nums, target) {',
      '  let left = 0;',
      '  let right = nums.length - 1;',
      '',
      '  while (left <= right) {',
      '    const mid = Math.floor((left + right) / 2);',
      '    if (nums[mid] === target) return mid;',
      '',
      '    if (nums[left] <= nums[mid]) {',
      '      if (target >= nums[left] && target < nums[mid]) right = mid - 1;',
      '      else left = mid + 1;',
      '    } else {',
      '      if (target > nums[mid] && target <= nums[right]) left = mid + 1;',
      '      else right = mid - 1;',
      '    }',
      '  }',
      '  return -1;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(log n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-17',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Longest Substring Without Repeating Characters',
    answer: [
      '**Problem:** Find the length of the longest substring without repeating characters.',
      '',
      '**Concept:** Use sliding window with two pointers and a Set to track characters.',
      '',
      '**Solution:**',
      '```javascript',
      'function lengthOfLongestSubstring(s) {',
      '  const seen = new Set();',
      '  let left = 0;',
      '  let maxLength = 0;',
      '',
      '  for (let right = 0; right < s.length; right++) {',
      '    while (seen.has(s[right])) {',
      '      seen.delete(s[left]);',
      '      left++;',
      '    }',
      '    seen.add(s[right]);',
      '    maxLength = Math.max(maxLength, right - left + 1);',
      '  }',
      '  return maxLength;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(min(m, n)) where m is charset size',
    ].join('\n'),
  },
  {
    id: 'dsa-18',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Merge Intervals',
    answer: [
      '**Problem:** Given an array of intervals, merge all overlapping intervals.',
      '',
      '**Concept:** Sort intervals by start time, then merge overlapping ones.',
      '',
      '**Solution:**',
      '```javascript',
      'function merge(intervals) {',
      '  if (intervals.length <= 1) return intervals;',
      '',
      '  intervals.sort((a, b) => a[0] - b[0]);',
      '  const result = [intervals[0]];',
      '',
      '  for (let i = 1; i < intervals.length; i++) {',
      '    const current = intervals[i];',
      '    const last = result[result.length - 1];',
      '',
      '    if (current[0] <= last[1]) {',
      '      last[1] = Math.max(last[1], current[1]);',
      '    } else {',
      '      result.push(current);',
      '    }',
      '  }',
      '  return result;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n log n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-19',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Group Anagrams',
    answer: [
      '**Problem:** Group anagrams together from an array of strings.',
      '',
      '**Concept:** Use sorted string as key to group anagrams.',
      '',
      '**Solution:**',
      '```javascript',
      'function groupAnagrams(strs) {',
      '  const groups = new Map();',
      '',
      '  for (let str of strs) {',
      "    const key = str.split('').sort().join('');",
      '    if (!groups.has(key)) groups.set(key, []);',
      '    groups.get(key).push(str);',
      '  }',
      '  return Array.from(groups.values());',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n * m log m) where m is average string length',
      '- **Space Complexity:** O(n * m)',
    ].join('\n'),
  },
  {
    id: 'dsa-20',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Valid Palindrome',
    answer: [
      '**Problem:** Determine if a string is a palindrome, considering only alphanumeric characters and ignoring cases.',
      '',
      '**Concept:** Use two pointers from both ends, comparing characters while skipping non-alphanumeric ones.',
      '',
      '**Solution:**',
      '```javascript',
      'function isPalindrome(s) {',
      '  let left = 0;',
      '  let right = s.length - 1;',
      '',
      '  while (left < right) {',
      '    while (left < right && !/^[a-zA-Z0-9]$/.test(s[left])) left++;',
      '    while (left < right && !/^[a-zA-Z0-9]$/.test(s[right])) right--;',
      '    if (s[left].toLowerCase() !== s[right].toLowerCase()) return false;',
      '    left++;',
      '    right--;',
      '  }',
      '  return true;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-21',
    topicId: 'dsa',
    level: 'senior',
    questionType: 'coding',
    question: 'Serialize and Deserialize Binary Tree',
    answer: [
      '**Problem:** Design algorithms to serialize and deserialize a binary tree.',
      '',
      '**Concept:** Use preorder traversal with null markers for serialization.',
      '',
      '**Solution:**',
      '```javascript',
      'function serialize(root) {',
      '  const result = [];',
      '  function preorder(node) {',
      "    if (node === null) { result.push('null'); return; }",
      '    result.push(node.val.toString());',
      '    preorder(node.left);',
      '    preorder(node.right);',
      '  }',
      '  preorder(root);',
      "  return result.join(',');",
      '}',
      '',
      'function deserialize(data) {',
      "  const values = data.split(',');",
      '  let index = 0;',
      '  function buildTree() {',
      "    if (values[index] === 'null') { index++; return null; }",
      '    const node = { val: parseInt(values[index++]) };',
      '    node.left = buildTree();',
      '    node.right = buildTree();',
      '    return node;',
      '  }',
      '  return buildTree();',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(n)',
    ].join('\n'),
  },
  {
    id: 'dsa-22',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Kth Largest Element in Array',
    answer: [
      '**Problem:** Find the kth largest element in an unsorted array.',
      '',
      '**Concept:** Use quickselect algorithm or min-heap for efficient solution.',
      '',
      '**Solution (Quick Select):**',
      '```javascript',
      'function findKthLargest(nums, k) {',
      '  function quickSelect(left, right, k) {',
      '    if (left === right) return nums[left];',
      '    const pivotIndex = partition(left, right);',
      '    if (pivotIndex === k) return nums[k];',
      '    else if (pivotIndex < k) return quickSelect(pivotIndex + 1, right, k);',
      '    else return quickSelect(left, pivotIndex - 1, k);',
      '  }',
      '',
      '  function partition(left, right) {',
      '    const pivot = nums[right];',
      '    let i = left;',
      '    for (let j = left; j < right; j++) {',
      '      if (nums[j] >= pivot) {',
      '        [nums[i], nums[j]] = [nums[j], nums[i]];',
      '        i++;',
      '      }',
      '    }',
      '    [nums[i], nums[right]] = [nums[right], nums[i]];',
      '    return i;',
      '  }',
      '  return quickSelect(0, nums.length - 1, k - 1);',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n) average, O(n²) worst',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-23',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Top K Frequent Elements',
    answer: [
      '**Problem:** Find the k most frequent elements in an array.',
      '',
      '**Concept:** Count frequencies, then use bucket sort or heap to find top k elements.',
      '',
      '**Solution:**',
      '```javascript',
      'function topKFrequent(nums, k) {',
      '  const freqMap = new Map();',
      '  for (let num of nums) freqMap.set(num, (freqMap.get(num) || 0) + 1);',
      '',
      '  const buckets = Array(nums.length + 1).fill(null).map(() => []);',
      '  for (let [num, freq] of freqMap) buckets[freq].push(num);',
      '',
      '  const result = [];',
      '  for (let i = buckets.length - 1; i >= 0 && result.length < k; i--) {',
      '    result.push(...buckets[i]);',
      '  }',
      '  return result.slice(0, k);',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(n)',
    ].join('\n'),
  },
  {
    id: 'dsa-24',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Course Schedule',
    answer: [
      '**Problem:** Determine if you can finish all courses given prerequisites.',
      '',
      '**Concept:** Model as directed graph and detect cycles using DFS or topological sort.',
      '',
      '**Solution:**',
      '```javascript',
      'function canFinish(numCourses, prerequisites) {',
      '  const graph = Array(numCourses).fill(null).map(() => []);',
      '  for (let [course, prereq] of prerequisites) graph[prereq].push(course);',
      '',
      '  const visited = new Array(numCourses).fill(0);',
      '',
      '  function hasCycle(course) {',
      '    if (visited[course] === 1) return true;',
      '    if (visited[course] === 2) return false;',
      '    visited[course] = 1;',
      '    for (let nextCourse of graph[course]) {',
      '      if (hasCycle(nextCourse)) return true;',
      '    }',
      '    visited[course] = 2;',
      '    return false;',
      '  }',
      '',
      '  for (let i = 0; i < numCourses; i++) {',
      '    if (hasCycle(i)) return false;',
      '  }',
      '  return true;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(V + E)',
      '- **Space Complexity:** O(V + E)',
    ].join('\n'),
  },
  {
    id: 'dsa-25',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Word Break',
    answer: [
      '**Problem:** Determine if a string can be segmented into dictionary words.',
      '',
      '**Concept:** Use dynamic programming where dp[i] represents if string up to index i can be segmented.',
      '',
      '**Solution:**',
      '```javascript',
      'function wordBreak(s, wordDict) {',
      '  const wordSet = new Set(wordDict);',
      '  const dp = new Array(s.length + 1).fill(false);',
      '  dp[0] = true;',
      '',
      '  for (let i = 1; i <= s.length; i++) {',
      '    for (let j = 0; j < i; j++) {',
      '      if (dp[j] && wordSet.has(s.substring(j, i))) {',
      '        dp[i] = true;',
      '        break;',
      '      }',
      '    }',
      '  }',
      '  return dp[s.length];',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n²)',
      '- **Space Complexity:** O(n)',
    ].join('\n'),
  },
  {
    id: 'dsa-26',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Coin Change',
    answer: [
      '**Problem:** Find the minimum number of coins needed to make up a given amount.',
      '',
      '**Concept:** Use dynamic programming where dp[i] represents minimum coins needed for amount i.',
      '',
      '**Solution:**',
      '```javascript',
      'function coinChange(coins, amount) {',
      '  const dp = new Array(amount + 1).fill(Infinity);',
      '  dp[0] = 0;',
      '',
      '  for (let i = 1; i <= amount; i++) {',
      '    for (let coin of coins) {',
      '      if (coin <= i) {',
      '        dp[i] = Math.min(dp[i], dp[i - coin] + 1);',
      '      }',
      '    }',
      '  }',
      '  return dp[amount] === Infinity ? -1 : dp[amount];',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(amount * coins.length)',
      '- **Space Complexity:** O(amount)',
    ].join('\n'),
  },
  {
    id: 'dsa-27',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Longest Common Subsequence',
    answer: [
      '**Problem:** Find the length of the longest common subsequence between two strings.',
      '',
      '**Concept:** Use 2D DP where dp[i][j] represents LCS length for first i characters of text1 and first j characters of text2.',
      '',
      '**Solution:**',
      '```javascript',
      'function longestCommonSubsequence(text1, text2) {',
      '  const m = text1.length;',
      '  const n = text2.length;',
      '  const dp = Array(m + 1).fill(null).map(() => Array(n + 1).fill(0));',
      '',
      '  for (let i = 1; i <= m; i++) {',
      '    for (let j = 1; j <= n; j++) {',
      '      if (text1[i - 1] === text2[j - 1]) {',
      '        dp[i][j] = dp[i - 1][j - 1] + 1;',
      '      } else {',
      '        dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);',
      '      }',
      '    }',
      '  }',
      '  return dp[m][n];',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(m * n)',
      '- **Space Complexity:** O(m * n)',
    ].join('\n'),
  },
  {
    id: 'dsa-28',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Number of Islands',
    answer: [
      "**Problem:** Count the number of islands in a 2D grid where '1' represents land and '0' represents water.",
      '',
      '**Concept:** Use DFS or BFS to explore connected land cells and mark them as visited.',
      '',
      '**Solution:**',
      '```javascript',
      'function numIslands(grid) {',
      '  if (!grid || grid.length === 0) return 0;',
      '',
      '  const rows = grid.length;',
      '  const cols = grid[0].length;',
      '  let islands = 0;',
      '',
      '  function dfs(row, col) {',
      "    if (row < 0 || row >= rows || col < 0 || col >= cols || grid[row][col] === '0') return;",
      "    grid[row][col] = '0';",
      '    dfs(row + 1, col);',
      '    dfs(row - 1, col);',
      '    dfs(row, col + 1);',
      '    dfs(row, col - 1);',
      '  }',
      '',
      '  for (let i = 0; i < rows; i++) {',
      '    for (let j = 0; j < cols; j++) {',
      "      if (grid[i][j] === '1') {",
      '        islands++;',
      '        dfs(i, j);',
      '      }',
      '    }',
      '  }',
      '  return islands;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(m * n)',
      '- **Space Complexity:** O(m * n) for recursion stack',
    ].join('\n'),
  },
  {
    id: 'dsa-29',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Design LRU Cache',
    answer: [
      '**Problem:** Design a data structure that follows LRU (Least Recently Used) cache constraints.',
      '',
      '**Concept:** Use HashMap for O(1) access and doubly linked list for O(1) insertion/deletion.',
      '',
      '**Solution:**',
      '```javascript',
      'class LRUCache {',
      '  constructor(capacity) {',
      '    this.capacity = capacity;',
      '    this.cache = new Map();',
      '    this.head = { key: 0, val: 0 };',
      '    this.tail = { key: 0, val: 0 };',
      '    this.head.next = this.tail;',
      '    this.tail.prev = this.head;',
      '  }',
      '',
      '  get(key) {',
      '    if (this.cache.has(key)) {',
      '      const node = this.cache.get(key);',
      '      this.moveToHead(node);',
      '      return node.val;',
      '    }',
      '    return -1;',
      '  }',
      '',
      '  put(key, value) {',
      '    if (this.cache.has(key)) {',
      '      const node = this.cache.get(key);',
      '      node.val = value;',
      '      this.moveToHead(node);',
      '    } else {',
      '      const newNode = { key, val: value };',
      '      if (this.cache.size >= this.capacity) {',
      '        const tail = this.removeTail();',
      '        this.cache.delete(tail.key);',
      '      }',
      '      this.cache.set(key, newNode);',
      '      this.addToHead(newNode);',
      '    }',
      '  }',
      '',
      '  addToHead(node) {',
      '    node.prev = this.head;',
      '    node.next = this.head.next;',
      '    this.head.next.prev = node;',
      '    this.head.next = node;',
      '  }',
      '',
      '  removeNode(node) {',
      '    node.prev.next = node.next;',
      '    node.next.prev = node.prev;',
      '  }',
      '',
      '  moveToHead(node) {',
      '    this.removeNode(node);',
      '    this.addToHead(node);',
      '  }',
      '',
      '  removeTail() {',
      '    const lastNode = this.tail.prev;',
      '    this.removeNode(lastNode);',
      '    return lastNode;',
      '  }',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(1) for both get and put',
      '- **Space Complexity:** O(capacity)',
    ].join('\n'),
  },
  {
    id: 'dsa-30',
    topicId: 'dsa',
    level: 'senior',
    questionType: 'coding',
    question: 'Sliding Window Maximum',
    answer: [
      '**Problem:** Find the maximum element in each sliding window of size k.',
      '',
      '**Concept:** Use deque to maintain indices of elements in decreasing order of their values.',
      '',
      '**Solution:**',
      '```javascript',
      'function maxSlidingWindow(nums, k) {',
      '  const result = [];',
      '  const deque = [];',
      '',
      '  for (let i = 0; i < nums.length; i++) {',
      '    while (deque.length > 0 && deque[0] < i - k + 1) deque.shift();',
      '    while (deque.length > 0 && nums[deque[deque.length - 1]] < nums[i]) deque.pop();',
      '    deque.push(i);',
      '    if (i >= k - 1) result.push(nums[deque[0]]);',
      '  }',
      '  return result;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(k)',
    ].join('\n'),
  },
  {
    id: 'dsa-31',
    topicId: 'dsa',
    level: 'senior',
    questionType: 'coding',
    question: 'Trapping Rain Water',
    answer: [
      '**Problem:** Calculate how much water can be trapped after raining given an elevation map.',
      '',
      '**Concept:** Use two pointers approach with left and right maximum heights.',
      '',
      '**Solution:**',
      '```javascript',
      'function trap(height) {',
      '  if (height.length === 0) return 0;',
      '',
      '  let left = 0;',
      '  let right = height.length - 1;',
      '  let leftMax = 0;',
      '  let rightMax = 0;',
      '  let water = 0;',
      '',
      '  while (left < right) {',
      '    if (height[left] < height[right]) {',
      '      if (height[left] >= leftMax) {',
      '        leftMax = height[left];',
      '      } else {',
      '        water += leftMax - height[left];',
      '      }',
      '      left++;',
      '    } else {',
      '      if (height[right] >= rightMax) {',
      '        rightMax = height[right];',
      '      } else {',
      '        water += rightMax - height[right];',
      '      }',
      '      right--;',
      '    }',
      '  }',
      '  return water;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-32',
    topicId: 'dsa',
    level: 'senior',
    questionType: 'coding',
    question: 'Edit Distance',
    answer: [
      '**Problem:** Find the minimum number of operations to convert word1 to word2.',
      '',
      '**Concept:** Use 2D DP where dp[i][j] represents minimum operations to convert first i characters of word1 to first j characters of word2.',
      '',
      '**Solution:**',
      '```javascript',
      'function minDistance(word1, word2) {',
      '  const m = word1.length;',
      '  const n = word2.length;',
      '  const dp = Array(m + 1).fill(null).map(() => Array(n + 1).fill(0));',
      '',
      '  for (let i = 0; i <= m; i++) dp[i][0] = i;',
      '  for (let j = 0; j <= n; j++) dp[0][j] = j;',
      '',
      '  for (let i = 1; i <= m; i++) {',
      '    for (let j = 1; j <= n; j++) {',
      '      if (word1[i - 1] === word2[j - 1]) {',
      '        dp[i][j] = dp[i - 1][j - 1];',
      '      } else {',
      '        dp[i][j] = Math.min(',
      '          dp[i - 1][j] + 1,',
      '          dp[i][j - 1] + 1,',
      '          dp[i - 1][j - 1] + 1',
      '        );',
      '      }',
      '    }',
      '  }',
      '  return dp[m][n];',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(m * n)',
      '- **Space Complexity:** O(m * n)',
    ].join('\n'),
  },
  {
    id: 'dsa-33',
    topicId: 'dsa',
    level: 'senior',
    questionType: 'coding',
    question: 'Median of Two Sorted Arrays',
    answer: [
      '**Problem:** Find the median of two sorted arrays.',
      '',
      '**Concept:** Use binary search on the smaller array to find the correct partition.',
      '',
      '**Solution:**',
      '```javascript',
      'function findMedianSortedArrays(nums1, nums2) {',
      '  if (nums1.length > nums2.length) {',
      '    [nums1, nums2] = [nums2, nums1];',
      '  }',
      '',
      '  const m = nums1.length;',
      '  const n = nums2.length;',
      '  let left = 0;',
      '  let right = m;',
      '',
      '  while (left <= right) {',
      '    const partitionX = Math.floor((left + right) / 2);',
      '    const partitionY = Math.floor((m + n + 1) / 2) - partitionX;',
      '',
      '    const maxLeftX = partitionX === 0 ? -Infinity : nums1[partitionX - 1];',
      '    const minRightX = partitionX === m ? Infinity : nums1[partitionX];',
      '    const maxLeftY = partitionY === 0 ? -Infinity : nums2[partitionY - 1];',
      '    const minRightY = partitionY === n ? Infinity : nums2[partitionY];',
      '',
      '    if (maxLeftX <= minRightY && maxLeftY <= minRightX) {',
      '      if ((m + n) % 2 === 0) {',
      '        return (Math.max(maxLeftX, maxLeftY) + Math.min(minRightX, minRightY)) / 2;',
      '      } else {',
      '        return Math.max(maxLeftX, maxLeftY);',
      '      }',
      '    } else if (maxLeftX > minRightY) {',
      '      right = partitionX - 1;',
      '    } else {',
      '      left = partitionX + 1;',
      '    }',
      '  }',
      "  throw new Error('Input arrays are not sorted');",
      '}',
      '```',
      '',
      '- **Time Complexity:** O(log(min(m, n)))',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-34',
    topicId: 'dsa',
    level: 'senior',
    questionType: 'coding',
    question: 'Regular Expression Matching',
    answer: [
      "**Problem:** Implement regular expression matching with support for '.' and '*'",
      '',
      '**Concept:** Use 2D DP where dp[i][j] represents if first i characters of string match first j characters of pattern.',
      '',
      '**Solution:**',
      '```javascript',
      'function isMatch(s, p) {',
      '  const m = s.length;',
      '  const n = p.length;',
      '  const dp = Array(m + 1).fill(null).map(() => Array(n + 1).fill(false));',
      '',
      '  dp[0][0] = true;',
      '  for (let j = 2; j <= n; j += 2) {',
      "    if (p[j - 1] === '*') dp[0][j] = dp[0][j - 2];",
      '  }',
      '',
      '  for (let i = 1; i <= m; i++) {',
      '    for (let j = 1; j <= n; j++) {',
      "      if (p[j - 1] === '*') {",
      "        dp[i][j] = dp[i][j - 2] || (dp[i - 1][j] && (s[i - 1] === p[j - 2] || p[j - 2] === '.'));",
      '      } else {',
      "        dp[i][j] = dp[i - 1][j - 1] && (s[i - 1] === p[j - 1] || p[j - 1] === '.');",
      '      }',
      '    }',
      '  }',
      '  return dp[m][n];',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(m * n)',
      '- **Space Complexity:** O(m * n)',
    ].join('\n'),
  },
  {
    id: 'dsa-35',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Longest Palindromic Substring',
    answer: [
      '**Problem:** Find the longest palindromic substring in a given string.',
      '',
      '**Concept:** Expand around centers approach: for each possible center, expand outward while characters match.',
      '',
      '**Solution:**',
      '```javascript',
      'function longestPalindrome(s) {',
      "  if (s.length === 0) return '';",
      '',
      '  let start = 0;',
      '  let maxLength = 1;',
      '',
      '  function expandAroundCenter(left, right) {',
      '    while (left >= 0 && right < s.length && s[left] === s[right]) {',
      '      const currentLength = right - left + 1;',
      '      if (currentLength > maxLength) {',
      '        start = left;',
      '        maxLength = currentLength;',
      '      }',
      '      left--;',
      '      right++;',
      '    }',
      '  }',
      '',
      '  for (let i = 0; i < s.length; i++) {',
      '    expandAroundCenter(i, i);',
      '    expandAroundCenter(i, i + 1);',
      '  }',
      '  return s.substring(start, start + maxLength);',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n²)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-36',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Binary Tree Level Order Traversal',
    answer: [
      '**Problem:** Given the root of a binary tree, return the level order traversal of its nodes values.',
      '',
      '**Concept:** Use a queue for Breadth-First Search (BFS). Process each level by tracking the number of nodes currently in the queue.',
      '',
      '**Solution:**',
      '```javascript',
      'function levelOrder(root) {',
      '  if (!root) return [];',
      '  const result = [];',
      '  const queue = [root];',
      '  while (queue.length > 0) {',
      '    const levelSize = queue.length;',
      '    const currentLevel = [];',
      '    for (let i = 0; i < levelSize; i++) {',
      '      const node = queue.shift();',
      '      currentLevel.push(node.val);',
      '      if (node.left) queue.push(node.left);',
      '      if (node.right) queue.push(node.right);',
      '    }',
      '    result.push(currentLevel);',
      '  }',
      '  return result;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(n)',
    ].join('\n'),
  },
  {
    id: 'dsa-37',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Validate Binary Search Tree',
    answer: [
      '**Problem:** Determine if a binary tree is a valid binary search tree (BST).',
      '',
      '**Concept:** Use recursion with a valid range (min, max) for each node. For the left child, the max becomes the current node value; for the right child, the min becomes the current node value.',
      '',
      '**Solution:**',
      '```javascript',
      'function isValidBST(root, min = -Infinity, max = Infinity) {',
      '  if (!root) return true;',
      '  if (root.val <= min || root.val >= max) return false;',
      '  return isValidBST(root.left, min, root.val) && isValidBST(root.right, root.val, max);',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(h)',
    ].join('\n'),
  },
  {
    id: 'dsa-38',
    topicId: 'dsa',
    level: 'junior',
    questionType: 'coding',
    question: 'Path Sum',
    answer: [
      '**Problem:** Given the root of a binary tree and an integer `targetSum`, return true if the tree has a root-to-leaf path such that adding up all the values along the path equals `targetSum`.',
      '',
      '**Concept:** Use DFS. Subtract the current node value from `targetSum` and recursively check children. A path is valid if we reach a leaf where the remaining sum equals the leaf value.',
      '',
      '**Solution:**',
      '```javascript',
      'function hasPathSum(root, targetSum) {',
      '  if (!root) return false;',
      '  if (!root.left && !root.right) return targetSum === root.val;',
      '  return hasPathSum(root.left, targetSum - root.val) || hasPathSum(root.right, targetSum - root.val);',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(h)',
    ].join('\n'),
  },
  {
    id: 'dsa-39',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Subsets (Power Set)',
    answer: [
      '**Problem:** Given an integer array `nums` of unique elements, return all possible subsets (the power set).',
      '',
      '**Concept:** Use backtracking. For each element, we have two choices: include it in the current subset or not.',
      '',
      '**Solution:**',
      '```javascript',
      'function subsets(nums) {',
      '  const result = [];',
      '  function backtrack(start, current) {',
      '    result.push([...current]);',
      '    for (let i = start; i < nums.length; i++) {',
      '      current.push(nums[i]);',
      '      backtrack(i + 1, current);',
      '      current.pop();',
      '    }',
      '  }',
      '  backtrack(0, []);',
      '  return result;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n * 2ⁿ)',
      '- **Space Complexity:** O(n)',
    ].join('\n'),
  },
  {
    id: 'dsa-40',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Permutations',
    answer: [
      '**Problem:** Given an array `nums` of distinct integers, return all the possible permutations.',
      '',
      '**Concept:** Use backtracking to swap elements or use a "visited" set to track used elements and build paths of length `nums.length`.',
      '',
      '**Solution:**',
      '```javascript',
      'function permute(nums) {',
      '  const result = [];',
      '  function backtrack(current) {',
      '    if (current.length === nums.length) {',
      '      result.push([...current]);',
      '      return;',
      '    }',
      '    for (let num of nums) {',
      '      if (current.includes(num)) continue;',
      '      current.push(num);',
      '      backtrack(current);',
      '      current.pop();',
      '    }',
      '  }',
      '  backtrack([]);',
      '  return result;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n * n!)',
      '- **Space Complexity:** O(n)',
    ].join('\n'),
  },
  {
    id: 'dsa-41',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Longest Increasing Subsequence',
    answer: [
      '**Problem:** Given an integer array `nums`, return the length of the longest strictly increasing subsequence.',
      '',
      '**Concept:** Use DP. `dp[i]` is the length of the LIS ending at index `i`. For each `i`, check all `j < i`. If `nums[i] > nums[j]`, then `dp[i] = max(dp[i], dp[j] + 1)`.',
      '',
      '**Solution:**',
      '```javascript',
      'function lengthOfLIS(nums) {',
      '  if (nums.length === 0) return 0;',
      '  const dp = new Array(nums.length).fill(1);',
      '  for (let i = 1; i < nums.length; i++) {',
      '    for (let j = 0; j < i; j++) {',
      '      if (nums[i] > nums[j]) dp[i] = Math.max(dp[i], dp[j] + 1);',
      '    }',
      '  }',
      '  return Math.max(...dp);',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n²)',
      '- **Space Complexity:** O(n)',
    ].join('\n'),
  },
  {
    id: 'dsa-42',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Unique Paths',
    answer: [
      '**Problem:** A robot is at the top-left corner of a `m x n` grid. It can only move down or right. How many unique paths are there to the bottom-right corner?',
      '',
      '**Concept:** Use DP. The number of ways to reach `(i, j)` is `paths(i-1, j) + paths(i, j-1)`. Initialize first row and column with 1.',
      '',
      '**Solution:**',
      '```javascript',
      'function uniquePaths(m, n) {',
      '  const dp = Array(m).fill(null).map(() => Array(n).fill(1));',
      '  for (let i = 1; i < m; i++) {',
      '    for (let j = 1; j < n; j++) {',
      '      dp[i][j] = dp[i-1][j] + dp[i][j-1];',
      '    }',
      '  }',
      '  return dp[m-1][n-1];',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(m * n)',
      '- **Space Complexity:** O(m * n)',
    ].join('\n'),
  },
  {
    id: 'dsa-43',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Clone Graph',
    answer: [
      '**Problem:** Given a reference of a node in a connected undirected graph, return a deep copy of the graph.',
      '',
      '**Concept:** Use a Map to store original node to cloned node mapping (to handle cycles). Traverse with DFS or BFS.',
      '',
      '**Solution:**',
      '```javascript',
      'function cloneGraph(node, map = new Map()) {',
      '  if (!node) return null;',
      '  if (map.has(node)) return map.get(node);',
      '  const copy = { val: node.val, neighbors: [] };',
      '  map.set(node, copy);',
      '  for (let neighbor of node.neighbors) {',
      '    copy.neighbors.push(cloneGraph(neighbor, map));',
      '  }',
      '  return copy;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(V + E)',
      '- **Space Complexity:** O(V)',
    ].join('\n'),
  },
  {
    id: 'dsa-44',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Lowest Common Ancestor of a Binary Tree',
    answer: [
      '**Problem:** Find the lowest common ancestor (LCA) of two given nodes in a binary tree.',
      '',
      '**Concept:** Recursively search left and right subtrees. If a subtree returns a node, it means one of the targets was found. If both subtrees return nodes, the current node is the LCA.',
      '',
      '**Solution:**',
      '```javascript',
      'function lowestCommonAncestor(root, p, q) {',
      '  if (!root || root === p || root === q) return root;',
      '  const left = lowestCommonAncestor(root.left, p, q);',
      '  const right = lowestCommonAncestor(root.right, p, q);',
      '  if (left && right) return root;',
      '  return left || right;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(h)',
    ].join('\n'),
  },
  {
    id: 'dsa-45',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Spiral Matrix',
    answer: [
      '**Problem:** Given an `m x n` matrix, return all elements of the matrix in spiral order.',
      '',
      '**Concept:** Use four boundaries: top, bottom, left, and right. Iterate and shrink the boundaries until they cross.',
      '',
      '**Solution:**',
      '```javascript',
      'function spiralOrder(matrix) {',
      '  const res = [];',
      '  let top = 0, bottom = matrix.length - 1;',
      '  let left = 0, right = matrix[0].length - 1;',
      '  while (top <= bottom && left <= right) {',
      '    for (let i = left; i <= right; i++) res.push(matrix[top][i]);',
      '    top++;',
      '    for (let i = top; i <= bottom; i++) res.push(matrix[i][right]);',
      '    right--;',
      '    if (top <= bottom) {',
      '      for (let i = right; i >= left; i--) res.push(matrix[bottom][i]);',
      '      bottom--;',
      '    }',
      '    if (left <= right) {',
      '      for (let i = bottom; i >= top; i--) res.push(matrix[i][left]);',
      '      left++;',
      '    }',
      '  }',
      '  return res;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(m * n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-46',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Rotting Oranges',
    answer: [
      '**Problem:** Given a grid where 0 is empty, 1 is fresh orange, and 2 is rotten orange, find the minimum time until all oranges rot.',
      '',
      '**Concept:** Multi-source BFS. Start BFS from all initial rotten oranges. Each step increases time and rots adjacent fresh oranges.',
      '',
      '**Solution:**',
      '```javascript',
      'function orangesRotting(grid) {',
      '  let queue = [], fresh = 0, time = 0;',
      '  for (let r = 0; r < grid.length; r++) {',
      '    for (let c = 0; c < grid[0].length; c++) {',
      '      if (grid[r][c] === 2) queue.push([r, c]);',
      '      if (grid[r][c] === 1) fresh++;',
      '    }',
      '  }',
      '  const dirs = [[0, 1], [0, -1], [1, 0], [-1, 0]];',
      '  while (queue.length && fresh) {',
      '    let next = [];',
      '    for (let [r, c] of queue) {',
      '      for (let [dr, dc] of dirs) {',
      '        let nr = r + dr, nc = c + dc;',
      '        if (grid[nr]?.[nc] === 1) {',
      '          grid[nr][nc] = 2; fresh--; next.push([nr, nc]);',
      '        }',
      '      }',
      '    }',
      '    queue = next; time++;',
      '  }',
      '  return fresh ? -1 : time;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(m * n)',
      '- **Space Complexity:** O(m * n)',
    ].join('\n'),
  },
  {
    id: 'dsa-47',
    topicId: 'dsa',
    level: 'senior',
    questionType: 'coding',
    question: 'Word Search',
    answer: [
      '**Problem:** Given a board of characters and a string `word`, return true if the word exists in the grid.',
      '',
      '**Concept:** Use backtracking with DFS. For each cell, if it matches the first letter, explore its neighbors. Temporarily mark the cell as visited (e.g., using a special character) to avoid using the same cell twice in one path.',
      '',
      '**Solution:**',
      '```javascript',
      'function exist(board, word) {',
      '  function dfs(r, c, i) {',
      '    if (i === word.length) return true;',
      '    if (!board[r]?.[c] || board[r][c] !== word[i]) return false;',
      '    const char = board[r][c];',
      "    board[r][c] = '#';",
      '    const found = dfs(r+1, c, i+1) || dfs(r-1, c, i+1) || dfs(r, c+1, i+1) || dfs(r, c-1, i+1);',
      '    board[r][c] = char;',
      '    return found;',
      '  }',
      '  for (let r = 0; r < board.length; r++)',
      '    for (let c = 0; c < board[0].length; c++)',
      '      if (dfs(r, c, 0)) return true;',
      '  return false;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(N * 3ᴸ) where N is cells and L is word length',
      '- **Space Complexity:** O(L)',
    ].join('\n'),
  },
  {
    id: 'dsa-48',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Find All Anagrams in a String',
    answer: [
      '**Problem:** Given strings `s` and `p`, find all start indices of `p`s anagrams in `s`.',
      '',
      '**Concept:** Sliding window with a frequency map. Move the window of size `p.length` across `s` and compare character counts.',
      '',
      '**Solution:**',
      '```javascript',
      'function findAnagrams(s, p) {',
      '  const res = [], map = {};',
      '  for (let char of p) map[char] = (map[char] || 0) + 1;',
      '  let left = 0, right = 0, count = p.length;',
      '  while (right < s.length) {',
      '    if (map[s[right++]]-- >= 1) count--;',
      '    if (count === 0) res.push(left);',
      '    if (right - left === p.length && map[s[left++]]++ >= 0) count++;',
      '  }',
      '  return res;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-49',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Container With Most Water',
    answer: [
      '**Problem:** Given an array of heights, find two lines that together with the x-axis form a container that holds the most water.',
      '',
      '**Concept:** Use two pointers at the ends. Calculate area, then move the pointer pointing to the shorter line (to potentially find a taller line).',
      '',
      '**Solution:**',
      '```javascript',
      'function maxArea(height) {',
      '  let left = 0, right = height.length - 1, max = 0;',
      '  while (left < right) {',
      '    const area = Math.min(height[left], height[right]) * (right - left);',
      '    max = Math.max(max, area);',
      '    height[left] < height[right] ? left++ : right--;',
      '  }',
      '  return max;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-50',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Letter Combinations of a Phone Number',
    answer: [
      '**Problem:** Given a string containing digits from 2-9, return all possible letter combinations that the number could represent.',
      '',
      '**Concept:** Backtracking. Map each digit to its letters and recursively build combinations.',
      '',
      '**Solution:**',
      '```javascript',
      'function letterCombinations(digits) {',
      '  if (!digits) return [];',
      "  const map = { 2:'abc', 3:'def', 4:'ghi', 5:'jkl', 6:'mno', 7:'pqrs', 8:'tuv', 9:'wxyz' };",
      '  const res = [];',
      '  function backtrack(i, s) {',
      '    if (i === digits.length) { res.push(s); return; }',
      '    for (let char of map[digits[i]]) backtrack(i + 1, s + char);',
      '  }',
      '  backtrack(0, "");',
      '  return res;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(4ⁿ)',
      '- **Space Complexity:** O(n)',
    ].join('\n'),
  },
  {
    id: 'dsa-51',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Set Matrix Zeroes',
    answer: [
      '**Problem:** Given an `m x n` matrix, if an element is 0, set its entire row and column to 0. Do it in-place.',
      '',
      '**Concept:** Use the first row and first column of the matrix itself as markers to track which rows and columns need to be zeroed.',
      '',
      '**Solution:**',
      '```javascript',
      'function setZeroes(matrix) {',
      '  let firstRow = false, firstCol = false;',
      '  for (let r = 0; r < matrix.length; r++)',
      '    for (let c = 0; c < matrix[0].length; c++)',
      '      if (matrix[r][c] === 0) {',
      '        if (r === 0) firstRow = true;',
      '        if (c === 0) firstCol = true;',
      '        matrix[0][c] = 0; matrix[r][0] = 0;',
      '      }',
      '  for (let r = 1; r < matrix.length; r++)',
      '    for (let c = 1; c < matrix[0].length; c++)',
      '      if (matrix[r][0] === 0 || matrix[0][c] === 0) matrix[r][c] = 0;',
      '  if (firstRow) for (let c = 0; c < matrix[0].length; c++) matrix[0][c] = 0;',
      '  if (firstCol) for (let r = 0; r < matrix.length; r++) matrix[r][0] = 0;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(m * n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-52',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: "Topological Sort (Kahn's Algorithm)",
    answer: [
      '**Problem:** Given a directed acyclic graph (DAG), return a linear ordering of vertices such that for every directed edge `uv`, vertex `u` comes before `v`.',
      '',
      '**Concept:** Calculate in-degrees of all nodes. Start with nodes having in-degree 0. Remove them and decrease in-degrees of their neighbors. Repeat.',
      '',
      '**Solution:**',
      '```javascript',
      'function topSort(numNodes, edges) {',
      '  const adj = Array(numNodes).fill(0).map(() => []);',
      '  const inDegree = Array(numNodes).fill(0);',
      '  for (let [u, v] of edges) { adj[u].push(v); inDegree[v]++; }',
      '  const queue = [];',
      '  for (let i = 0; i < numNodes; i++) if (inDegree[i] === 0) queue.push(i);',
      '  const res = [];',
      '  while (queue.length) {',
      '    const u = queue.shift(); res.push(u);',
      '    for (let v of adj[u]) if (--inDegree[v] === 0) queue.push(v);',
      '  }',
      '  return res.length === numNodes ? res : [];',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(V + E)',
      '- **Space Complexity:** O(V + E)',
    ].join('\n'),
  },
  {
    id: 'dsa-53',
    topicId: 'dsa',
    level: 'senior',
    questionType: 'coding',
    question: 'Minimum Window Substring',
    answer: [
      '**Problem:** Given strings `s` and `t`, find the minimum window in `s` which contains all characters of `t`.',
      '',
      '**Concept:** Sliding window with two pointers. Expand `right` until the window is valid, then contract `left` as much as possible while maintaining validity.',
      '',
      '**Solution:**',
      '```javascript',
      'function minWindow(s, t) {',
      '  let map = {}, count = 0, left = 0, min = "";',
      '  for (let c of t) { if (!map[c]) count++; map[c] = (map[c] || 0) + 1; }',
      '  for (let right = 0; right < s.length; right++) {',
      '    if (--map[s[right]] === 0) count--;',
      '    while (count === 0) {',
      '      if (!min || right - left + 1 < min.length) min = s.slice(left, right + 1);',
      '      if (map[s[left++]]++ === 0) count++;',
      '    }',
      '  }',
      '  return min;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(k) where k is charset size',
    ].join('\n'),
  },
  {
    id: 'dsa-54',
    topicId: 'dsa',
    level: 'senior',
    questionType: 'coding',
    question: 'Binary Tree Maximum Path Sum',
    answer: [
      '**Problem:** Find the maximum path sum in a binary tree. The path may start and end at any node.',
      '',
      '**Concept:** For each node, calculate the maximum gain it can contribute to a path (max(0, left_gain) + max(0, right_gain) + node_val). Track the overall maximum.',
      '',
      '**Solution:**',
      '```javascript',
      'function maxPathSum(root) {',
      '  let max = -Infinity;',
      '  function gain(node) {',
      '    if (!node) return 0;',
      '    const left = Math.max(0, gain(node.left));',
      '    const right = Math.max(0, gain(node.right));',
      '    max = Math.max(max, left + right + node.val);',
      '    return Math.max(left, right) + node.val;',
      '  }',
      '  gain(root); return max;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(h)',
    ].join('\n'),
  },
  {
    id: 'dsa-55',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Non-overlapping Intervals',
    answer: [
      '**Problem:** Find the minimum number of intervals to remove to make the rest non-overlapping.',
      '',
      '**Concept:** Greedy. Sort by end time. Always keep the interval that ends earliest to leave more room for others.',
      '',
      '**Solution:**',
      '```javascript',
      'function eraseOverlapIntervals(intervals) {',
      '  intervals.sort((a, b) => a[1] - b[1]);',
      '  let count = 0, lastEnd = -Infinity;',
      '  for (let [start, end] of intervals) {',
      '    if (start >= lastEnd) lastEnd = end;',
      '    else count++;',
      '  }',
      '  return count;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n log n)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-56',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Rotate Image (Matrix)',
    answer: [
      '**Problem:** Rotate an `n x n` 2D matrix 90 degrees clockwise in-place.',
      '',
      '**Concept:** Transpose the matrix (swap `matrix[i][j]` with `matrix[j][i]`), then reverse each row.',
      '',
      '**Solution:**',
      '```javascript',
      'function rotate(matrix) {',
      '  for (let i = 0; i < matrix.length; i++)',
      '    for (let j = i + 1; j < matrix.length; j++)',
      '      [matrix[i][j], matrix[j][i]] = [matrix[j][i], matrix[i][j]];',
      '  for (let row of matrix) row.reverse();',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n²)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
  {
    id: 'dsa-57',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Trie (Prefix Tree)',
    answer: [
      '**Problem:** Implement a Trie with `insert`, `search`, and `startsWith` methods.',
      '',
      '**Concept:** Each node has a children map and an `isEnd` boolean.',
      '',
      '**Solution:**',
      '```javascript',
      'class Trie {',
      '  constructor() { this.root = {}; }',
      '  insert(word) {',
      '    let node = this.root;',
      '    for (let c of word) node = node[c] = node[c] || {};',
      '    node.isEnd = true;',
      '  }',
      '  search(word) {',
      '    let node = this.root;',
      '    for (let c of word) if (!(node = node[c])) return false;',
      '    return !!node.isEnd;',
      '  }',
      '  startsWith(prefix) {',
      '    let node = this.root;',
      '    for (let c of prefix) if (!(node = node[c])) return false;',
      '    return true;',
      '  }',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(L) for all operations',
      '- **Space Complexity:** O(Total characters)',
    ].join('\n'),
  },
  {
    id: 'dsa-58',
    topicId: 'dsa',
    level: 'senior',
    questionType: 'coding',
    question: 'Longest Valid Parentheses',
    answer: [
      '**Problem:** Find the length of the longest valid parentheses substring.',
      '',
      '**Concept:** Use a stack. Initialize with -1. For "(", push index. For ")", pop. If stack empty, push current index. Else, `max = max(max, i - stack[top])`.',
      '',
      '**Solution:**',
      '```javascript',
      'function longestValidParentheses(s) {',
      '  const stack = [-1]; let max = 0;',
      '  for (let i = 0; i < s.length; i++) {',
      '    if (s[i] === "(") stack.push(i);',
      '    else {',
      '      stack.pop();',
      '      if (!stack.length) stack.push(i);',
      '      else max = Math.max(max, i - stack[stack.length - 1]);',
      '    }',
      '  }',
      '  return max;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(n)',
      '- **Space Complexity:** O(n)',
    ].join('\n'),
  },
  {
    id: 'dsa-59',
    topicId: 'dsa',
    level: 'senior',
    questionType: 'coding',
    question: 'Kth Smallest Element in a BST',
    answer: [
      '**Problem:** Find the kth smallest element in a binary search tree.',
      '',
      '**Concept:** Inorder traversal of a BST visits nodes in ascending order. Count nodes during traversal.',
      '',
      '**Solution:**',
      '```javascript',
      'function kthSmallest(root, k) {',
      '  let res;',
      '  function inorder(node) {',
      '    if (!node || res !== undefined) return;',
      '    inorder(node.left);',
      '    if (--k === 0) { res = node.val; return; }',
      '    inorder(node.right);',
      '  }',
      '  inorder(root); return res;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(h + k)',
      '- **Space Complexity:** O(h)',
    ].join('\n'),
  },
  {
    id: 'dsa-60',
    topicId: 'dsa',
    level: 'mid',
    questionType: 'coding',
    question: 'Valid Sudoku',
    answer: [
      '**Problem:** Determine if a 9x9 Sudoku board is valid.',
      '',
      '**Concept:** Use three sets of sets (for rows, columns, and 3x3 boxes) to track seen digits.',
      '',
      '**Solution:**',
      '```javascript',
      'function isValidSudoku(board) {',
      '  const rows = Array(9).fill(0).map(() => new Set());',
      '  const cols = Array(9).fill(0).map(() => new Set());',
      '  const boxes = Array(9).fill(0).map(() => new Set());',
      '  for (let r = 0; r < 9; r++)',
      '    for (let c = 0; c < 9; c++) {',
      '      const v = board[r][c];',
      "      if (v === '.') continue;",
      '      const b = Math.floor(r/3) * 3 + Math.floor(c/3);',
      '      if (rows[r].has(v) || cols[c].has(v) || boxes[b].has(v)) return false;',
      '      rows[r].add(v); cols[c].add(v); boxes[b].add(v);',
      '    }',
      '  return true;',
      '}',
      '```',
      '',
      '- **Time Complexity:** O(1) (fixed 81 cells)',
      '- **Space Complexity:** O(1)',
    ].join('\n'),
  },
];
