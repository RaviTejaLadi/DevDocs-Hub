# TypeScript

TypeScript adds static typing on top of JavaScript. It helps catch errors early,
improves IDE support, and makes large applications easier to maintain.

---

## Basic Types

| Type      | Example                        | Description             |
| --------- | ------------------------------ | ----------------------- |
| string    | `let name: string = "Ravi"`    | Text value              |
| number    | `let age: number = 25`         | Integer/float           |
| boolean   | `let active: boolean = true`   | True/false              |
| null      | `let data: null = null`        | Null value              |
| undefined | `let value: undefined`         | Undefined value         |
| bigint    | `let id: bigint = 100n`        | Large numbers           |
| symbol    | `const key: symbol = Symbol()` | Unique identifier       |
| any       | `let x: any`                   | Disable type checking   |
| unknown   | `let x: unknown`               | Safe alternative to any |
| never     | `function fail(): never`       | Never returns           |
| void      | `function log(): void`         | No return value         |

// Example demonstrating TypeScript.
```ts
// Basic types
let username: string = 'John';
let age: number = 25;
let isAdmin: boolean = false;
```

## Type Inference

| Syntax                | Result  |
| --------------------- | ------- |
| `let name = "John"`   | string  |
| `let age = 25`        | number  |
| `const active = true` | boolean |

// Example demonstrating TypeScript.
```ts
const message = 'Hello';
// TypeScript infers string automatically
```

## Arrays

| Syntax      | Example                |
| ----------- | ---------------------- |
| Type[]      | `string[]`             |
| Array<Type> | `Array<string>`        |
| Mixed Array | `(string \| number)[]` |

// Example demonstrating TypeScript.
```ts
const users: string[] = ['John', 'Alex'];
const ids: Array<number> = [1, 2, 3];
```

## Tuples

| Syntax         | Example                 |
| -------------- | ----------------------- |
| Fixed Types    | `[string, number]`      |
| Optional Value | `[string, number?]`     |
| Rest Element   | `[string, ...number[]]` |

// Example demonstrating TypeScript.
```ts
const user: [string, number] = ['John', 25];
```

## Objects

// Example demonstrating TypeScript.
```ts
const user: {
  name: string;
  age: number;
} = {
  name: 'John',
  age: 25,
};
```

| Syntax            | Example               |
| ----------------- | --------------------- |
| Object Type       | `{name:string}`       |
| Nested Object     | `{user:{id:number}}`  |
| Optional Property | `name?: string`       |
| Readonly          | `readonly id: number` |

## Functions

| Syntax         | Example            |
| -------------- | ------------------ |
| Return Type    | `(): string`       |
| Void Function  | `(): void`         |
| Optional Param | `name?: string`    |
| Default Param  | `name = "Guest"`   |
| Rest Param     | `...args:number[]` |

// Example demonstrating TypeScript.
```ts
// Function with types
function greet(name: string): string {
  return `Hello ${name}`;
}
```

## Function Type

// Example demonstrating TypeScript.
```ts
let calculate: (a: number, b: number) => number;

calculate = (a, b) => a + b;
```

## Interfaces

| Syntax             | Example              |
| ------------------ | -------------------- |
| Interface          | `interface User {}`  |
| Optional Property  | `name?: string`      |
| Readonly Property  | `readonly id:number` |
| Function Signature | `login(): void`      |

// Example demonstrating TypeScript.
```ts
interface User {
  id: number;
  name: string;
  email?: string;
}
```

## Type Alias

// Example demonstrating TypeScript.
```ts
type User = {
  id: number;
  name: string;
};
```

| Type Alias | Example                              |
| ---------- | ------------------------------------ |
| Primitive  | `type ID = string`                   |
| Object     | `type User = {}`                     |
| Union      | `type Status = "success" \| "error"` |

## Interface vs Type

| Interface           | Type          |
| ------------------- | ------------- |
| Extendable          | Yes           |
| Declaration Merging | Yes           |
| Union Support       | No            |
| Complex Types       | Limited       |
| Recommended For     | Object shapes |

## Union Types

// Example demonstrating TypeScript.
```ts
type ID = string | number;
```

| Example            | Meaning         |
| ------------------ | --------------- |
| `string \| number` | Either          |
| `"yes" \| "no"`    | Literal values  |
| `User \| null`     | Nullable object |

## Intersection Types

// Example demonstrating TypeScript.
```ts
type User = {
  name: string;
};

type Admin = {
  permissions: string[];
};

type SuperAdmin = User & Admin;
```

## Literal Types

// Example demonstrating TypeScript.
```ts
type Theme = 'light' | 'dark';
```

// Example demonstrating TypeScript.
```ts
let theme: Theme = 'light';
```

## Enums

// Example demonstrating TypeScript.
```ts
enum Role {
  Admin,
  User,
  Guest,
}
```

### String Enum

// Example demonstrating TypeScript.
```ts
enum Status {
  Success = 'SUCCESS',
  Error = 'ERROR',
}
```

## Type Assertions

// Example demonstrating TypeScript.
```ts
const input = document.getElementById('name') as HTMLInputElement;

input.value = 'John';
```

### Alternative

// Example demonstrating TypeScript.
```ts
const input = <HTMLInputElement>document.getElementById('name');
```

## Type Narrowing

// Example demonstrating TypeScript.
```ts
function print(id: string | number) {
  if (typeof id === 'string') {
    console.log(id.toUpperCase());
  }
}
```

### Common Narrowing

| Method       | Example                     |
| ------------ | --------------------------- |
| typeof       | `typeof value === "string"` |
| instanceof   | `value instanceof Date`     |
| in           | `"name" in obj`             |
| Custom Guard | `isUser(data)`              |

## Type Guards

// Example demonstrating TypeScript.
```ts
interface User {
  name: string;
}

function isUser(data: any): data is User {
  return data?.name !== undefined;
}
```

## Generics

Generics create reusable type-safe code.

// Example demonstrating TypeScript.
```ts
function identity<T>(value: T): T {
  return value;
}
```

// Example demonstrating TypeScript.
```ts
identity<string>('hello');
identity<number>(100);
```

## Generic Interfaces

// Example demonstrating TypeScript.
```ts
interface ApiResponse<T> {
  success: boolean;
  data: T;
}
```

// Example demonstrating TypeScript.
```ts
const response: ApiResponse<User> = {
  success: true,
  data: {
    id: 1,
    name: 'John',
  },
};
```

## Generic Constraints

// Example demonstrating TypeScript.
```ts
interface HasId {
  id: number;
}

function getId<T extends HasId>(item: T) {
  return item.id;
}
```

## keyof

// Example demonstrating TypeScript.
```ts
interface User {
  name: string;
  age: number;
}

type Keys = keyof User;
```

Result:

// Example demonstrating TypeScript.
```ts
'name' | 'age';
```

## typeof

// Example demonstrating TypeScript.
```ts
const user = {
  name: 'John',
  age: 25,
};

type UserType = typeof user;
```

## Mapped Types

// Example demonstrating TypeScript.
```ts
type User = {
  name: string;
  age: number;
};

type ReadonlyUser = {
  readonly [K in keyof User]: User[K];
};
```

## Partial

type PartialUser = Partial<User>;
// Example demonstrating TypeScript.
```

All properties become optional.

## Required

```ts
Required<User>;
// Example demonstrating TypeScript.
```

All properties required.

## Readonly

```ts
Readonly<User>;
// Example demonstrating TypeScript.
```

All properties readonly.

## Pick

```ts
type UserPreview = Pick<User, 'name'>;
// Example demonstrating TypeScript.
```

## Omit

```ts
type UserWithoutAge = Omit<User, 'age'>;
// Example demonstrating TypeScript.
```

## Record

```ts
type Users = Record<string, number>;
// Example demonstrating TypeScript.
```

```ts
{
  john: 1;
  alex: 2;
}
// Example demonstrating TypeScript.
```

## Exclude

```ts
type Status = Exclude<'success' | 'error' | 'loading', 'loading'>;
// Example demonstrating TypeScript.
```

## Extract

```ts
type Result = Extract<string | number, string>;
// Example demonstrating TypeScript.
```

## NonNullable

```ts
type Name = NonNullable<string | null | undefined>;
// Example demonstrating TypeScript.
```

## Classes

```ts
class User {
  constructor(public name: string, private age: number) {}
}
// Example demonstrating TypeScript.
```

## Access Modifiers

| Modifier  | Accessible    |
| --------- | ------------- |
| public    | Everywhere    |
| private   | Same class    |
| protected | Class + Child |
| readonly  | Read only     |

## Abstract Class

```ts
abstract class Animal {
  abstract speak(): void;
}
// Example demonstrating TypeScript.
```

## Async/Await

```ts
async function getUsers(): Promise<User[]> {
  const res = await fetch('/api/users');
  return res.json();
}
// Example demonstrating TypeScript.
```

## Promise

```ts
const promise: Promise<string> = Promise.resolve('Hello');
// Example demonstrating TypeScript.
```

### Export

```ts
export const API_URL = '/api';
// Example demonstrating TypeScript.
```

### Default Export

```ts
export default App;
// Example demonstrating TypeScript.
```

### Import

```ts
import App from './App';
// Example demonstrating TypeScript.
```

## Declaration Files

| File          | Purpose               |
| ------------- | --------------------- |
| `index.d.ts`  | Type definitions      |
| `global.d.ts` | Global types          |
| `env.d.ts`    | Environment variables |

## Common React Types

| Type        | Example                         |
| ----------- | ------------------------------- |
| ReactNode   | `children: ReactNode`           |
| FC          | `FC<Props>`                     |
| Dispatch    | `Dispatch<SetStateAction<T>>`   |
| MouseEvent  | `MouseEvent<HTMLButtonElement>` |
| ChangeEvent | `ChangeEvent<HTMLInputElement>` |
| RefObject   | `RefObject<HTMLInputElement>`   |

## tsconfig Important Options

| Option        | Purpose              |
| ------------- | -------------------- |
| strict        | Enable strict mode   |
| noImplicitAny | Prevent implicit any |
| target        | JS version           |
| module        | Module system        |
| jsx           | React JSX            |
| baseUrl       | Absolute imports     |
| paths         | Alias support        |
| skipLibCheck  | Faster builds        |

## Most Used Advanced Types

```ts
type Nullable<T> = T | null;

type ApiResponse<T> = {
  success: boolean;
  data: T;
};

type AsyncFunction<T> = () => Promise<T>;

type ValueOf<T> = T[keyof T];
// Example demonstrating TypeScript.
```

## Frequently Used Interview Concepts

| Concept            | Example                 |
| ------------------ | ----------------------- |
| Union Types        | `string \| number`      |
| Intersection Types | `User & Admin`          |
| Generics           | `function fn<T>()`      |
| keyof              | Property keys           |
| typeof             | Extract type from value |
| Utility Types      | Partial, Pick, Omit     |
| Type Guards        | `typeof`, `instanceof`  |
| Mapped Types       | `[K in keyof T]`        |
| Conditional Types  | `T extends U ? X : Y`   |
| Infer Keyword      | Extract nested types    |

## Conditional Types

```ts
type IsString<T> = T extends string ? true : false;

type Result = IsString<string>; // true
// Example demonstrating TypeScript.
```

## Infer Keyword

```ts
type ReturnTypeOf<T> = T extends (...args: any[]) => infer R ? R : never;
// Example demonstrating TypeScript.
```

## Useful TypeScript Patterns for React

```ts
// API Response
type ApiResponse<T> = {
  data: T;
  success: boolean;
  message: string;
};

// Component Props
type ButtonProps = {
  children: React.ReactNode;
  onClick: () => void;
};

// Generic Hook
function useLocalStorage<T>(key: string, initialValue: T) {
  // implementation
}
// Example demonstrating TypeScript.
```

## Quick Reference

| Area | Revision point |
|---|---|
| Definition | State exactly what the topic means. |
| Mechanism | Explain the steps that produce the result. |
| Example | Use a small realistic case. |
| Pitfalls | Know common mistakes and boundary cases. |
