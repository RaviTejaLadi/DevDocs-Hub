# ⏱ Time, Speed & Distance (TSD)

Time, Speed & Distance is one of the most important aptitude topics for
placements and competitive exams. Most questions are based on a few simple
formulas, but the challenge is applying them quickly.

Common question types:

- Basic Speed, Distance, Time
- Average Speed
- Relative Speed
- Trains
- Boats & Streams
- Circular Tracks
- Races

---

## Basic Concepts

There are three fundamental quantities:

- Distance (D)
- Speed (S)
- Time (T)

Relationship:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Distance = Speed × Time
```

From this:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Speed = Distance / Time
```

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Time = Distance / Speed
```

### Speed Conversion

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
1 m/s = 18/5 km/hr
```

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
1 km/hr = 5/18 m/s
```

## Example

Convert 72 km/hr into m/s.

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
72 × 5/18

= 20 m/s
```

✅ Answer: 20 m/s

Convert 15 m/s into km/hr.

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
15 × 18/5

= 54 km/hr
```

✅ Answer: 54 km/hr

## Example 1

A car travels at 60 km/hr for 3 hours.

Find distance.

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Distance = Speed × Time

= 60 × 3

= 180 km
```

✅ Answer: 180 km

## Example 2

A train covers 240 km in 4 hours.

Find speed.

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Speed = 240/4

= 60 km/hr
```

✅ Answer: 60 km/hr

## Average Speed

Many students make mistakes here.

Formula:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Average Speed

= Total Distance / Total Time
```

A car travels:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
60 km at 30 km/hr
```

and

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
60 km at 60 km/hr
```

Time taken:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
60/30 = 2 hr
```

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
60/60 = 1 hr
```

Total:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Distance = 120 km

Time = 3 hr
```

Average Speed:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
120/3

= 40 km/hr
```

✅ Answer: 40 km/hr

## Shortcut for Equal Distances

When equal distances are covered at speeds a and b:

= (2ab)/(a+b)
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

Speeds:

```text
30 km/hr and 60 km/hr
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
(2×30×60)/(30+60)

= 3600/90

## Relative Speed

Used when two objects move simultaneously.

## Same Direction

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Relative Speed

= Difference of Speeds
```

Example:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Car A = 60 km/hr
Car B = 40 km/hr
```

= 60 - 40

= 20 km/hr
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

✅ Answer: 20 km/hr

## Opposite Direction

= Sum of Speeds
```

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
50 km/hr and 70 km/hr
```

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
50 + 70

= 120 km/hr
```

✅ Answer: 120 km/hr

## Meeting Problems

Two cars start from opposite ends.

Distance:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
240 km
```

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
40 km/hr and 20 km/hr
```

Relative speed:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
40 + 20

Time to meet:

```text
240/60

= 4 hours
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

✅ Answer: 4 hours

## 🚂 Train Problems

Train questions are very common in placements.

## Pole Crossing

When a train crosses a pole:

Distance covered:

```text
Length of Train
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
Time

= Length of Train / Speed
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

Train length:

```text
120 m
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

Speed:

```text
54 km/hr
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

Convert speed:

```text
54 × 5/18

= 15 m/s
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

Time:

```text
120/15

= 8 sec
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

✅ Answer: 8 seconds

## Crossing a Platform

```text
Length of Train + Length of Platform
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

= (Train Length + Platform Length)/Speed
```

Train:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
150 m
```

Platform:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
250 m
```

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
20 m/s
```

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
150 + 250

= 400 m
```

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
400/20

= 20 sec
```

✅ Answer: 20 seconds

## Two Trains Crossing

= Sum of Lengths / Sum of Speeds
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```


Train A:

Train B:

```text
180 m
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
54 km/hr and 36 km/hr
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

Convert:

```text
54 → 15 m/s

36 → 10 m/s
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
120 + 180

= 300 m
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
15 + 10

= 25 m/s
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
300/25

= 12 sec
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

✅ Answer: 12 sec

= Sum of Lengths / Difference of Speeds
```

## 🌊 Boats and Streams

A favorite topic in aptitude exams.

### Downstream

Boat moves with current.

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Downstream Speed

= Boat Speed + Stream Speed
```

### Upstream

Boat moves against current.

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Upstream Speed

= Boat Speed - Stream Speed
```

## Finding Boat Speed

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Boat Speed

= (Downstream + Upstream)/2
```

Downstream:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
20 km/hr
```

Upstream:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
12 km/hr
```

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
(20 + 12)/2

= 16 km/hr
```

✅ Boat Speed = 16 km/hr

## Finding Stream Speed

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Stream Speed

= (Downstream - Upstream)/2
```

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
(20 - 12)/2

= 4 km/hr
```

✅ Stream Speed = 4 km/hr

## 🏃 Race Problems

A beats B by 100 meters.

Race length:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
500 m
```

Ratio:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
A:B = 500:400

= 5:4
```

Speed ratio:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
5 : 4
```

✅ Answer: 5:4

## Circular Track Problems

When two runners move on a circular track.

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Difference of Speeds
```

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
Sum of Speeds
```

Track length:

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
400 m
```

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
10 m/s and 6 m/s
```

Same direction:

= 4 m/s
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
400/4

= 100 sec
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

✅ Answer: 100 sec

## If Speed Increases

Time decreases.

```text
Speed ∝ 1/Time
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

Speed increases by:

```text
25%
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

New speed:

```text
125%
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

Time ratio:

```text
100 : 125

= 4 : 5
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

New time decreases by:

```text
20%
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

## Speed Change Formula

If speed increases by x%.

Time decrease:

```text
[x/(100+x)] × 100
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

Speed increases:

```text
25/125 × 100

= 20%
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

✅ Answer: 20%

## Important Ratios

| Speed Change | Time Change |
| ------------ | ----------- |
| +25%         | -20%        |
| +20%         | -16.67%     |
| +50%         | -33.33%     |
| +100%        | -50%        |

## Question 1

A car travels 360 km in 6 hours.

```text
360/6

## Question 2

Convert 90 km/hr into m/s.

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
90 × 5/18

✅ Answer: 25 m/s

## Question 3

A train 180 m long crosses a pole in 9 seconds.

```text
180/9

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
20 × 18/5

= 72 km/hr
```

✅ Answer: 72 km/hr

## Question 4

Downstream speed = 18 km/hr

Upstream speed = 10 km/hr

Find stream speed.

// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```text
(18-10)/2

✅ Answer: 4 km/hr

## Question 5

Two persons travel towards each other.

```text
300 km
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
40 km/hr and 35 km/hr
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
75 km/hr
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
300/75

= 4 hr
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

### Mistake 1

Using average of speeds as average speed.

Wrong:

```text
(30+60)/2

= 45
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

Correct:

```text
40 km/hr
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

when distances are equal.

### Mistake 2

Not converting units.

```text
Length → meters
Speed → km/hr
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

Always convert before solving.

### Mistake 3

Using sum instead of difference for same-direction trains.

Remember:

```text
Same Direction → Difference
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
Opposite Direction → Sum
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

## Exam Tips

Memorize:

Train formulas:

```text
Pole → Train Length
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
Platform → Train + Platform
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
Opposite Trains → Sum Lengths / Sum Speeds
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
Same Direction → Sum Lengths / Difference Speeds
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

Boats & Streams:

```text
Boat Speed = (D+U)/2
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

```text
Stream Speed = (D-U)/2
// Example demonstrating ⏱ Time, Speed & Distance (TSD).
```

These shortcuts solve most placement questions within seconds.

## ✅ Summary

Time, Speed & Distance revolves around three core formulas:

Important subtopics include:

- Average Speed
- Relative Speed
- Trains
- Boats & Streams
- Circular Tracks
- Race Problems
- Speed-Time Percentage Changes

Mastering these concepts will help you solve many placement-level aptitude
questions quickly and accurately.

## Quick Reference

| Area | Revision point |
|---|---|
| Definition | State exactly what the topic means. |
| Mechanism | Explain the steps that produce the result. |
| Example | Use a small realistic case. |
| Pitfalls | Know common mistakes and boundary cases. |
