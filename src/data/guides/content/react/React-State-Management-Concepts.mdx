## **Local State**

```jsx
// Component-specific state
function Counter() {
  const [count, setCount] = useState(0); // Local state

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>Increment</button>
    </div>
  );
}

// Form state
function LoginForm() {
  const [formData, setFormData] = useState({
    email: '',
    password: '',
    remember: false,
  });

  const handleChange = (e) => {
    const { name, value, type, checked } = e.target;
    setFormData((prev) => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value,
    }));
  };

  return (
    <form>
      <input name="email" value={formData.email} onChange={handleChange} />
      <input
        name="password"
        type="password"
        value={formData.password}
        onChange={handleChange}
      />
      <input
        name="remember"
        type="checkbox"
        checked={formData.remember}
        onChange={handleChange}
      />
    </form>
  );
}
```

## **Context API**

```jsx
// Creating context
const AuthContext = React.createContext(null);

// Provider component
function AuthProvider({ children }) {
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);

  const login = async (email, password) => {
    // Auth logic
    const user = await authService.login(email, password);
    setUser(user);
  };

  const logout = () => {
    authService.logout();
    setUser(null);
  };

  const value = { user, loading, login, logout };

  return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>;
}

// Custom hook for consuming
function useAuth() {
  const context = useContext(AuthContext);
  if (!context) {
    throw new Error('useAuth must be used within AuthProvider');
  }
  return context;
}

// Usage in components
function Profile() {
  const { user, logout } = useAuth();
  return (
    <div>
      <h1>Welcome, {user.name}</h1>
      <button onClick={logout}>Logout</button>
    </div>
  );
}

// Multiple contexts
const ThemeContext = React.createContext('light');
const LanguageContext = React.createContext('en');

function App() {
  return (
    <ThemeContext.Provider value="dark">
      <LanguageContext.Provider value="fr">
        <Component />
      </LanguageContext.Provider>
    </ThemeContext.Provider>
  );
}
```

## **Reducers**

```jsx
// Complex state management
const initialState = {
  items: [],
  loading: false,
  error: null,
  filters: { category: 'all', search: '' },
  pagination: { page: 1, total: 0 },
};

function reducer(state, action) {
  switch (action.type) {
    case 'FETCH_START':
      return { ...state, loading: true, error: null };
    case 'FETCH_SUCCESS':
      return {
        ...state,
        loading: false,
        items: action.payload.items,
        pagination: { ...state.pagination, total: action.payload.total },
      };
    case 'FETCH_ERROR':
      return { ...state, loading: false, error: action.payload };
    case 'SET_FILTER':
      return {
        ...state,
        filters: {
          ...state.filters,
          [action.payload.key]: action.payload.value,
        },
      };
    case 'SET_PAGE':
      return {
        ...state,
        pagination: { ...state.pagination, page: action.payload },
      };
    case 'CLEAR_ITEMS':
      return { ...state, items: [] };
    default:
      return state;
  }
}

function ProductList() {
  const [state, dispatch] = useReducer(reducer, initialState);

  useEffect(() => {
    dispatch({ type: 'FETCH_START' });
    fetchProducts(state.filters, state.pagination.page)
      .then((data) => dispatch({ type: 'FETCH_SUCCESS', payload: data }))
      .catch((error) => dispatch({ type: 'FETCH_ERROR', payload: error }));
  }, [state.filters, state.pagination.page]);

  // Actions
  const setFilter = (key, value) => {
    dispatch({ type: 'SET_FILTER', payload: { key, value } });
  };

  const setPage = (page) => {
    dispatch({ type: 'SET_PAGE', payload: page });
  };

  // Combine with context
  const value = { state, setFilter, setPage };
  return <ProductContext.Provider value={value}>...</ProductContext.Provider>;
}
```

## **Lifting State Up**

```jsx
// Child components share state via parent
function TemperatureConverter() {
  const [celsius, setCelsius] = useState('');
  const [fahrenheit, setFahrenheit] = useState('');

  const handleCelsiusChange = (value) => {
    setCelsius(value);
    setFahrenheit(value ? ((parseFloat(value) * 9) / 5 + 32).toString() : '');
  };

  const handleFahrenheitChange = (value) => {
    setFahrenheit(value);
    setCelsius(value ? (((parseFloat(value) - 32) * 5) / 9).toString() : '');
  };

  return (
    <div>
      <TemperatureInput
        scale="c"
        value={celsius}
        onChange={handleCelsiusChange}
      />
      <TemperatureInput
        scale="f"
        value={fahrenheit}
        onChange={handleFahrenheitChange}
      />
    </div>
  );
}

function TemperatureInput({ scale, value, onChange }) {
  const label = scale === 'c' ? 'Celsius' : 'Fahrenheit';

  const handleChange = (e) => {
    onChange(e.target.value);
  };

  return (
    <div>
      <label>{label}</label>
      <input type="number" value={value} onChange={handleChange} />
    </div>
  );
}
```

## **Derived State**

```jsx
// Computed values from existing state
function ShoppingCart({ items, discount }) {
  // Derived state
  const subtotal = useMemo(
    () => items.reduce((sum, item) => sum + item.price * item.quantity, 0),
    [items]
  );

  const discountAmount = useMemo(
    () => subtotal * (discount / 100),
    [subtotal, discount]
  );

  const total = subtotal - discountAmount;
  const itemCount = items.reduce((sum, item) => sum + item.quantity, 0);

  return (
    <div>
      <p>Items: {itemCount}</p>
      <p>Subtotal: ${subtotal.toFixed(2)}</p>
      <p>Discount: -${discountAmount.toFixed(2)}</p>
      <p>Total: ${total.toFixed(2)}</p>
    </div>
  );
}

// State derived from props
function UserProfile({ userId, userData }) {
  // Derived from props
  const displayName = `${userData.firstName} ${userData.lastName}`;
  const isAdmin = userData.roles.includes('admin');

  return (
    <div>
      <h2>{displayName}</h2>
      {isAdmin && <Badge>Admin</Badge>}
    </div>
  );
}
```

## **Choosing the Right State Management**

```jsx
// Decision tree
function StateManagementGuide() {
  const decisions = {
    'Local State': {
      when: 'State used only by one component',
      example: 'Toggle state, form input',
      solution: 'useState, useReducer',
    },
    'Shared State': {
      when: 'State shared between few components',
      example: 'Parent-child communication',
      solution: 'Lifting state up, Context',
    },
    'Global State': {
      when: 'State used across many components',
      example: 'User auth, theme, language',
      solution: 'Context, Redux, Zustand',
    },
    'Server State': {
      when: 'Data from APIs, caching',
      example: 'API responses, mutations',
      solution: 'React Query, SWR',
    },
    'URL State': {
      when: 'Shareable, bookmarkable state',
      example: 'Filters, search, pagination',
      solution: 'React Router, Next.js router',
    },
    'Form State': {
      when: 'Complex forms with validation',
      example: 'Registration, checkout forms',
      solution: 'Formik, React Hook Form',
    },
  };

  return <div>{/* Decision tree UI */}</div>;
}

// Example: Combining approaches
function Application() {
  // URL state (React Router)
  const [searchParams] = useSearchParams();
  const page = parseInt(searchParams.get('page')) || 1;

  // Global state (Context)
  const { user } = useAuth();

  // Local state
  const [localFilters, setLocalFilters] = useState({});

  // Server state (React Query)
  const { data } = useQuery(['products', page, localFilters], fetchProducts);

  return <ProductList data={data} />;
}
```
